development:
  adapter: async

test:
  adapter: test

production:
  # Voterr intentionally runs one Puma worker in production. Keeping Action
  # Cable in that process avoids Solid Cable's 100 ms database polling loop.
  adapter: async
